import { useNavigate } from 'react-router-dom'
import './account.css'

function Account(){
    const navigate=useNavigate();

    return(
        <>
        <h1>Your account</h1>
        </>
    );
}

export default Account